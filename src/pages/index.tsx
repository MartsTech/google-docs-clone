import Head from "next/head";

const Home = () => {
  return (
    <div>
      <Head>
        <title>Google Docs Clone</title>
        <meta
          name="description"
          content="Google Docs Clone created using React"
        />
        <link rel="icon" href="/logo.svg" />
      </Head>
    </div>
  );
};

export default Home;
